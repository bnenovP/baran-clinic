pipeline {
    agent any

    tools {
        gradle 'gradle-baran-clinic'
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                withGradle() {
                    sh './gradlew clean test --exclude-tests "com.example.baranclinic.clinical.domain.repository*" --exclude-tests "com.example.baranclinic.crm.domain.repository*"'
                }
            }
        }
    }
}
