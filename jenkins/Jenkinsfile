pipeline {
    agent any 

    tools {
        gradle 'Gradle-9.3.1'
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                echo 'Running Unit Tests'
                withGradle() {
                    sh './gradlew clean test'
                }
            }
        }
    }
}
