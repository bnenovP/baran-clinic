pipeline {
    agent any

    tools {
        gradle 'gradle'
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                withGradle(gradleWrapperFile: './gradlew') {
                    sh './gradlew clean test --tests com.common.*'
                }
            }
        }
    }
}
