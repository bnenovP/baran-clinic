pipeline {
    agent any

    tools {
        gradle 'baran-clinic-gradle'
    }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                withGradle() {
                    sh './gradlew clean test --tests com.common.*'
                }
            }
        }
    }
}
