pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'gradle'
                    reuseNode true
                }
            steps {
                dir('srs/test') {
                    sh './gradlew clean test'
                }
            }
        }
    }
}
