pipeline {
    agent any

    stage {
        stage('Unit Tests - Backend') {
            steps {
                dir('srs/test') {
                    sh './gradlew clean test'
                }
            }
        }
    }
}